profiler_version: tpu-notebook-v1
vllm_version: n/a
gpu: TPU-v6e-1
hardware: TPU-v6e-1
profiled_at: '2026-05-11T16:00:00+00:00'
architecture: llama
model: meta-llama/Llama-3.2-1B-Instruct
variant: bf16
tp_degrees: [1]
engine_effective: {max_num_batched_tokens: 8192, max_num_seqs: 32, dtype: bfloat16, kv_cache_dtype: auto, block_size: 16,
  num_hidden_layers_profiled: 1, per_sequence_max_sequences: 8192}
dense_grid:
  tokens: {min: 1, max: 8192, step: 1, n_unique: 8192}
  layers: [act_fn, down_proj, embedding, final_layernorm, gate_up_proj, layernorm, o_proj, qkv_proj, rotary_emb]
per_sequence_grid:
  sequences: {min: 1, max: 8192, n_unique: 287}
  layers: [lm_head, sampler]
attention_grid:
  prefill_chunk: {min: 1, max: 8192, step: 1, n_unique: 8192}
  kv_prefill: [0]
  n_decode: [1, 2, 4, 8, 16, 32]
  kv_decode: {min: 1, max: 4096, step: 1, n_unique: 4096}
  kv_decode_max_per_batch: {1: 4096, 2: 4096, 4: 4096, 8: 4096, 16: 4096, 32: 4096}
  max_kv: 4096
skew_fit:
  per_tp:
    1: {method: synthetic-constant, alpha_default: 0.3}
calibration: {scaling_factor: 1.0, scaled_by: raw TPU notebook output (no scaling applied; .scaled_1.00.csv source)}
notes: "Converted from v1 TPU notebook CSV (single per-model CSV with layer_name,\ninput, kv_cache, latency(ns)) via /tmp/convert_tpu_v1_to_v2.py.\
  \ Mapping:\n  - layernorm  = input_layernorm + post_layernorm  (sum)\n  - qkv_proj   = q_proj + k_proj + v_proj        \
  \  (sum)\n  - gate_up_proj = gate_proj + up_proj             (sum)\n  - rotary_emb / sampler = analytic synthesize (no source\
  \ measurement)\nattention.csv key conversion:\n  - (input=N, kv=0)  -> (pc=N, kvp=0, n=0, kvd=0)   prefill\n  - (input=1,\
  \ kv=K)  -> (pc=0, kvp=0, n=1, kvd=K)   decode\nMeasurement floor (TPU notebook mechanism): ~40us dense, ~240us attention.\n\
  Small layers at small token counts are dominated by floor \u2014 see HISTORY note.\n"
